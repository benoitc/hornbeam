{erl_opts, [debug_info]}.

{deps, [
    {hornbeam, {git, "https://github.com/benoitc/hornbeam.git", {branch, "main"}}}
]}.

{shell, [
    {apps, [embedding_chat]}
]}.

%% For development, use local hornbeam
{overrides, [
    {override, hornbeam, [
        {deps, []}
    ]}
]}.

{project_plugins, [rebar3_hex]}.
